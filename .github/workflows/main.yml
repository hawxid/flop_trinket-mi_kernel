name: Interactive VPS Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source Kernel
        uses: actions/checkout@v4

      - name: Setup SSH Session (Tmate)
        uses: mxschmitt/action-tmate@v3
